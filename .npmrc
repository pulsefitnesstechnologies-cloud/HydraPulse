auto-install-peers=false
strict-peer-dependencies=false
frozen-lockfile=false
# Use a flat node_modules structure (like npm/yarn) so Metro bundler can
# resolve packages correctly in the pnpm monorepo. Metro does not handle
# pnpm's deep virtual store symlink structure well with React Native.
node-linker=hoisted
